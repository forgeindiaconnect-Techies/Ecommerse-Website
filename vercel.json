{
    "buildCommand": "npm install --prefix webapp && npm run --prefix webapp build",
    "outputDirectory": "webapp/dist",
    "cleanUrls": true,
    "trailingSlash": false,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}